---
import Header from "../sections/Header.astro";

interface Props {
  title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content="Risedata" />
    <meta name="viewport" content="width=device-width" />
    <title>{title}</title>
  </head>
  <body class="bg-blue text-neutral-50 font-sans text-base leading-loose">
    <Header />
    <slot />
    <script src="../lib/js/animations.js"></script>
    <script src="../lib/js/swipers.js"></script>
    <script
      is:inline
      type="module"
      src="https://cdn.jsdelivr.net/npm/@justinribeiro/lite-youtube@1/lite-youtube.min.js"
    ></script>
  </body>
</html>

<style is:global>
  h1,
  h2,
  h3,
  h4,
  h5,
  h6,
  p {
    @apply text-balance;
  }

  p {
    @apply mb-4;
  }

  hr {
    @apply border-neutral-50/50;
  }

  strong {
    @apply bg-red font-bold;
  }

  .feah {
    @apply -mt-20;
  }

  .swiper-button-disabled {
    @apply opacity-10;
  }
</style>
